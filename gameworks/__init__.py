"""
gameworks — Mine-Streaker Minesweeper game package.

Modules:
    engine   — Pure game logic (Board, GameEngine, cell states, mine placement)
    renderer — Pygame rendering (tiles, HUD, animations, image overlay)
    main     — CLI entry point & game loop state machine
"""

__version__ = "0.1.1"